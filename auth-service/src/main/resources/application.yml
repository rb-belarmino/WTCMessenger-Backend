spring:
  application:
    name: auth-service

  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://ygorcar:1234567890@cluster0.bbanf48.mongodb.net/wtc_messenger?appName=Cluster0}

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET:wtc-secret-key-change-in-production}
  access-expiration-ms: 900000
  refresh-expiration-ms: 604800000